{
  "name": "rescuesight",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm --workspace @rescuesight/web run dev",
    "dev:api": "npm --workspace @rescuesight/api run dev",
    "dev:mobile": "npm --workspace @rescuesight/mobile run dev",
    "dev:mobile:web": "npm --workspace @rescuesight/mobile run web",
    "build": "npm run build --workspaces",
    "typecheck": "npm run typecheck --workspaces",
    "test:api": "npm --workspace @rescuesight/api run test"
  }
}
